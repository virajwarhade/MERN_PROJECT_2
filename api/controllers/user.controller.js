export const test = (req,res) => {
    res.json({
        message: "API is working"
    });
};

export const test2 = (req,res) => {
    res.json({
        message: "Another API Working as well"
    });
};